%Mex File to compute running median in an efficient manner.
%Y=RNGMED(data,nblock);
%inputs:
%      data (array)
%      nblock (int): block size for computing median
%output:
%      out (array): containing running median (same size as data).
